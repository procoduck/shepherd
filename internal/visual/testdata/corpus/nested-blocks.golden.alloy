// generated by shepherd visual builder — do not edit by hand (edits will be overwritten); graph revision 4, schema alloy-v1.18.1

prometheus.scrape "app" {
  job_name = "myapp"
  scrape_interval = "30s"
}

prometheus.relabel "filter" {
  forward_to = [prometheus.scrape.app.metrics]
}

prometheus.remote_write "primary" {
  receiver = [prometheus.scrape.app.metrics]
}

prometheus.remote_write "secondary" {
  receiver = [prometheus.relabel.filter.receiver]
}
